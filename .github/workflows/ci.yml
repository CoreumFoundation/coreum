name: ci
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  schedule:
    - cron: '0 1 * * 1' # At 01:00 on Monday
  workflow_dispatch:

jobs:
  ci:
    strategy:
      fail-fast: false
      matrix:
        ci_step: [
          "lint",
          "test",
          "integration tests coreum-modules",
          "integration tests coreum-ibc",
          "integration tests coreum-upgrade",
          "integration tests faucet",
        ]
        include:
          - ci_step: "lint"
            command: "crust lint/coreum"
            go-cache: true
            wasm-cache: true
            linter-cache: true
            docker-cache: false
          - ci_step: "test"
            command: "crust test/coreum"
            go-cache: true
            wasm-cache: false
            linter-cache: false
            docker-cache: false
          - ci_step: "integration tests coreum-modules"
            command: "crust build/integration-tests/coreum/modules images && crust znet test --test-groups=coreum-modules"
            go-cache: true
            wasm-cache: true
            linter-cache: false
            docker-cache: true
          - ci_step: "integration tests coreum-ibc"
            command: "crust build/integration-tests/coreum/ibc images && crust znet test --test-groups=coreum-ibc"
            go-cache: true
            wasm-cache: false
            linter-cache: false
            docker-cache: true
          - ci_step: "integration tests coreum-upgrade"
            command: "crust build/integration-tests/coreum/upgrade build/integration-tests/coreum/modules build/integration-tests/coreum/ibc images && crust znet test --cored-version=v1.0.0 --test-groups=coreum-upgrade,coreum-modules,coreum-ibc"
            go-cache: true
            wasm-cache: true
            linter-cache: false
            docker-cache: true
          - ci_step: "integration tests faucet"
            command: "crust build/integration-tests/faucet images && crust znet test --test-groups=faucet"
            go-cache: true
            wasm-cache: false
            linter-cache: false
            docker-cache: true

    runs-on: ubuntu-22.04
    steps:
      - name: Go version used to build crust tool
        run: go version
      - name: Checkout coreum
        uses: actions/checkout@v3
        with:
          persist-credentials: false
          path: coreum
      - name: Checkout crust
        uses: actions/checkout@v3
        with:
          repository: CoreumFoundation/crust
          path: crust
      - name: Set up crust
        run: echo "$(pwd)/crust/bin" >> $GITHUB_PATH
      - name: Setup go cache
        uses: actions/cache@v3
        if: ${{ matrix.go-cache }}
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
      - name: Setup WASM cache
        uses: actions/cache@v3
        if: ${{ matrix.wasm-cache }}
        with:
          path: ~/.cache/crust/wasm
          key: ${{ runner.os }}-wasm-cache-${{ hashFiles('**/Cargo.lock') }}
      - name: Setup linter cache
        uses: actions/cache@v3
        if: ${{ matrix.linter-cache }}
        with:
          path: ~/.cache/golangci-lint
          key: ${{ runner.os }}-linter-cache
      - name: Get Date
        id: get-year-week
        run: |
          echo "date=$(/bin/date -u "+%Y-%U")" >> $GITHUB_OUTPUT
        shell: bash
      - name: Set docker cache
        uses: satackey/action-docker-layer-caching@v0.0.11
        if: ${{ matrix.docker-cache }}
        continue-on-error: true
        with:
          key: ${{ runner.os }}-docker-v2-${{ steps.get-year-week.outputs.date }} # year-week key
      - name: Set crust binaries cache
        uses: actions/cache@v3
        continue-on-error: true
        with:
          path: ~/.cache/crust/bin
          key: ${{ runner.os }}-bin-${{ steps.get-year-week.outputs.date }} # year-week key
      - name: Run ${{ matrix.ci_step }}
        run: ${{ matrix.command }}
      - name: Dump docker logs on failure
        if: failure()
        uses: jwalton/gh-docker-logs@v2
